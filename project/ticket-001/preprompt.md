# Ticket preprompt

- **Task ID**: ticket-001
- **Task title**: Bootstrap Performance standard and define delivery slices
- **Created**: 2026-08-14T12:14:16Z

Keep executable implementation outside this governance/evidence directory.
Read a human-owned user-*.md file only when one exists.
The request to execute this work creates SESSION_EXECUTION_AUTHORIZATION;
proceed within the recorded intent without a redundant confirmation prompt.
Require new authority for destructive action, secrets, external coordination,
material objective expansion and trusted merge approval.

## User objective

Create `wellmanifest/performance` as a reusable standard for performance
refactoring under multiple technical eventualities, starting with a governed
standalone repository.

## Technical directives

- Compose immutable Wellmanifest POA and DSL sources.
- Require measurements, workload identity, budgets, compatibility and rollback.
- Cover no-change, configuration, packaging, algorithm, concurrency, protocol,
  runtime, selective Rust extraction and replacement scenarios.
- Keep the standard descriptive and fail closed without authority.
- Keep implementation and tests outside ticket directories.
